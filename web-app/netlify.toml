# ── Build Settings ──
[build]
  command = "npm run build"
  publish = "../Output/web-app"

# ── SPA Redirect (wajib untuk React Router) ──
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
